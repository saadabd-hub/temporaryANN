// import { FakeBackend } from '../services/helper/fake-backend';

export class Games{
        image:string;
        nama:string;
        description:string;
        participant:string;
        matchday:string;
        type:string;
        id:string;
        join:string;
        next:string;
}
